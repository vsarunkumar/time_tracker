<%= link_to('<< Back to List', { action: 'index' }) %>
<div>
  <h2>Show Task</h2>
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>id</th>
        <td><%= @task.id %></td>
      </tr>
      <tr>
        <th>Project</th>
        <td><%= @task.project.name %></td>
      </tr>
      <tr>
        <th>Name</th>
        <td><%= @task.name %></td>
      </tr>
      <tr>
        <th>Hours</th>
        <td><%= @task.hour %></td>
      </tr>
    </thead>
  </table>
</div>