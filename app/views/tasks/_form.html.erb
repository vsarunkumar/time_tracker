<div class="container">
<%= form_for(task) do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :project_id %>
    <%= f.collection_select(:project_id, projects, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :hour %>
    <%= f.text_field :hour %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

</div>