<%= link_to('<< Back to List', { action: 'index' }) %>
<div>
  <h2>Delete Task</h2>
  <%= form_for(:task, :url => { action: 'destroy', id: @task.id}) %>
    <p>Are you sure want to permanently delete this Task?</p>

    <%= submit_tag('Delete Task')%>
  <% end %>
</div>